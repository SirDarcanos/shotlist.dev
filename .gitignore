# build and local validation output
dist/
artifacts/
# what a shoot writes before `--install` copies it where it belongs
screenshots/out/
# packaged from examples/shotlist-example/ by the build, so the folder stays the source of truth
public/shotlist-example.zip
# what a reader's own run of the example writes
examples/shotlist-example/public/images/
# generated types
.astro/
# local coding-agent task state
.pi/

# dependencies
node_modules/

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*


# environment variables
.env
.env.production

# macOS-specific files
.DS_Store

# jetbrains setting folder
.idea/
